spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vocaloid_game?serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true&autoReconnect=true&failOverReadOnly=false&maxReconnects=3&useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull
    username: USERNAME
    password: PASSWORD

server:
  port: 10001

# 文件上传配置
spring.servlet.multipart:
  max-file-size: 5MB
  max-request-size: 5MB
